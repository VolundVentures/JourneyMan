onlyBuiltDependencies:
  - "@prisma/engines"
  - "prisma"
  - "better-sqlite3"
  - "esbuild"
  - "sharp"

# Hoist auth packages so next-auth can find its peer deps (next, react)
public-hoist-pattern[]=next-auth
public-hoist-pattern[]=@auth/*
